<div class="modal fade" id="confirmShowModal" tabindex="-1" aria-labelledby="confirmShowModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="confirmShowModalLabel">Confirmation to show <%= field %></h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        Are you sure you want to show this <% if(field === "category") { %> <%= field %> <% } %>?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <form action="/admin/categories/<% if(field === "category") { %>show-category<% } else if (field === "product") { %>show-product<% }%>" method="post">
          <input type="hidden" value="<% if(field === "category") { %><%= valueId %><% } %>" name="<% if(field === "category") { %>categoryId<% } %>" />
          <button type="submit" class="btn btn-success">Show</button>
        </form>
      </div>
    </div>
  </div>
</div>